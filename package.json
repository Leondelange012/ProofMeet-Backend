{
  "name": "proofmeet-backend",
  "version": "1.0.0",
  "description": "ProofMeet backend API server",
  "main": "server-with-db.js",
  "scripts": {
    "start": "npx prisma migrate deploy && npx prisma generate && node server-with-db.js",
    "build": "npx prisma generate",
    "dev": "node server-with-db.js",
    "db:migrate": "npx prisma migrate dev",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@prisma/client": "^5.4.2"
  },
  "devDependencies": {
    "prisma": "^5.4.2"
  },
  "engines": {
    "node": "18.x"
  }
}
